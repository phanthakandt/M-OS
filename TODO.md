# TODO

- [ ] KikuChat: รองรับการแสดงรูปภาพในข้อความแชท (ตอนนี้ `ChatMessageData` มีแค่ `sender`/`text`/`timestamp`, และ `KikuChatApp._add_message_bubble()` render แค่ text bubble เท่านั้น — ยังไม่มี field หรือโค้ดรองรับรูปภาพเลย)
